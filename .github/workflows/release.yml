name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v5  # Используйте v4, если он работает, или обновитесь до v5/v6 при необходимости

      - name: Setup Node.js
        uses: actions/setup-node@v5  # Используйте v4, или обновитесь до v5
        with:
          node-version: '22.x'      # Явно указываем Node.js 22

      - name: Get version from tag
        id: get_version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_ENV

      - name: Update package.json version
        run: npm version ${{ env.VERSION }} --no-git-tag-version --no-commit-hooks

      - name: Pack module
        run: npm pack

      - name: Create Release
        uses: softprops/action-gh-release@v2  # Используем v2, которая поддерживает Node.js 24
        with:
          files: '*.tgz'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
